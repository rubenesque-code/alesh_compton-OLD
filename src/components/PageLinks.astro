---
import type { PageRoute } from "~types/index";

const links: PageRoute[] = [
  "squatumentary",
  "brexit",
  // "painting",
  {
    label: "Estat Du France",
    route: "estat-du-france",
  },
  {
    label: "Painters Touch",
    route: "painters-touch",
  },
  /*   {
    label: "Various Photo",
    route: "various-photo",
  }, */
  {
    label: "Various Video",
    route: "various-video",
  },
];
---

{
  links.map((link, i) =>
    i === 0 ? (
      <a href={"/"}>{link}</a>
    ) : typeof link === "string" ? (
      <a href={"/" + link}>{link}</a>
    ) : (
      <a href={"/" + link.route}>{link.label}</a>
    )
  )
}
